in the matlab file for the real sense you will find lines which reading
prechange = csvread("E:example\path\to\file(x)")*10^3;
and
change = csvread("E:example\path\to\file(x)")*10^3;

It extremly improtant that you replace all! instances of this with these lines replacing the important sections 
fileID1 = fopen('A:\location\of\kam\files\KAM Software\KAM-master\Kinect v2 3.0\sequences\nameofexperiment\average\number','r');
prechange = fread(fileID1, [512, 424], 'float32');
fclose(fileID1);

fileID2 = fopen('A:\location\of\kam\files\KAM Software\KAM-master\Kinect v2 3.0\sequences\nameofexperiment\average\number','r');
change = fread(fileID2, [512, 424], 'float32');
fclose(fileID2);

then the code should run the same, if you have any issues please leave a comment here so we can help!
